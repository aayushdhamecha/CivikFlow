import { CitizenLayout } from "@/components/civic/CitizenLayout";
import { PriorityBadge } from "@/components/civic/PriorityBadge";
import { StatusBadge } from "@/components/civic/StatusBadge";
import { trpc } from "@/lib/trpc";
import { ArrowRight, Bell, ClipboardList, MapPinned, Plus, ShieldCheck } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Link } from "wouter";

export default function CitizenHome() {
  const reports = trpc.civic.complaints.mine.useQuery();
  const notifications = trpc.civic.notifications.list.useQuery();
  const recent = reports.data?.slice(0, 3) ?? [];
  const active = reports.data?.filter(report => !["RESOLVED", "CLOSED", "REJECTED"].includes(report.status)).length ?? 0;
  const unread = notifications.data?.filter(item => !item.readAt).length ?? 0;
  return <CitizenLayout><main className="mx-auto max-w-6xl px-4 pb-24 pt-7 sm:px-6 sm:pt-10"><section className="relative overflow-hidden rounded-3xl bg-slate-950 px-6 py-8 text-white sm:px-10 sm:py-10"><div className="absolute -right-16 -top-24 h-64 w-64 rounded-full bg-emerald-500/25 blur-3xl" /><div className="relative max-w-xl"><span className="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1 text-xs font-bold text-emerald-200"><ShieldCheck className="h-3.5 w-3.5" /> Your civic action hub</span><h1 className="mt-4 text-3xl font-bold tracking-tight sm:text-4xl">See a problem? Start a clear path to action.</h1><p className="mt-3 text-sm leading-6 text-slate-300 sm:text-base">Send evidence, confirm the exact location, and follow verified updates from the responsible team.</p><Link href="/report"><Button size="lg" className="mt-6 bg-emerald-500 text-slate-950 hover:bg-emerald-400"><Plus className="mr-2 h-4 w-4" />Report a problem</Button></Link></div></section><section className="mt-6 grid gap-3 sm:grid-cols-3"><div className="rounded-2xl border border-slate-200 bg-white p-5"><ClipboardList className="h-5 w-5 text-emerald-700" /><p className="mt-4 text-3xl font-bold">{reports.data?.length ?? 0}</p><p className="mt-1 text-sm text-slate-600">Reports submitted</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5"><MapPinned className="h-5 w-5 text-blue-700" /><p className="mt-4 text-3xl font-bold">{active}</p><p className="mt-1 text-sm text-slate-600">Currently active</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5"><Bell className="h-5 w-5 text-amber-600" /><p className="mt-4 text-3xl font-bold">{unread}</p><p className="mt-1 text-sm text-slate-600">Unread updates</p></div></section><section className="mt-9"><div className="flex items-end justify-between"><div><p className="text-sm font-bold uppercase tracking-[.14em] text-emerald-700">Your reports</p><h2 className="mt-1 text-2xl font-bold tracking-tight">Recent activity</h2></div><Link href="/complaints" className="inline-flex items-center gap-1 text-sm font-bold text-emerald-700 hover:text-emerald-900">View all <ArrowRight className="h-4 w-4" /></Link></div>{reports.isLoading ? <div className="mt-5 grid gap-3"><div className="h-28 animate-pulse rounded-2xl bg-slate-200" /><div className="h-28 animate-pulse rounded-2xl bg-slate-200" /></div> : recent.length ? <div className="mt-5 grid gap-3">{recent.map(report => <Link key={report.id} href={`/complaints/${report.publicId}`} className="group rounded-2xl border border-slate-200 bg-white p-5 transition hover:border-emerald-300 hover:shadow-sm"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><div className="flex flex-wrap items-center gap-2"><p className="font-bold">{report.title}</p><StatusBadge status={report.status} /></div><p className="mt-2 text-sm text-slate-500">{report.publicId} · Submitted {new Date(report.createdAt).toLocaleDateString()}</p></div><div className="flex items-center gap-3"><PriorityBadge priority={report.priority} /><ArrowRight className="h-4 w-4 text-slate-400 transition-transform group-hover:translate-x-1" /></div></div></Link>)}</div> : <div className="mt-5 rounded-2xl border border-dashed border-slate-300 bg-white px-6 py-10 text-center"><MapPinned className="mx-auto h-8 w-8 text-emerald-700" /><h3 className="mt-3 font-bold">No reports yet</h3><p className="mt-1 text-sm text-slate-600">Report a local civic issue and track its progress here.</p><Link href="/report"><Button className="mt-5 bg-emerald-700 hover:bg-emerald-800">Report a problem</Button></Link></div>}</section></main></CitizenLayout>;
}
